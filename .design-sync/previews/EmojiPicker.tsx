import { EmojiPicker } from 'bora-frontend';

export function TreinoSelecionado() {
  return <EmojiPicker value="🏋️" onChange={() => {}} />;
}

export function AguaSelecionada() {
  return <EmojiPicker value="💧" onChange={() => {}} />;
}

export function CorridaSelecionada() {
  return <EmojiPicker value="🏃" onChange={() => {}} />;
}
